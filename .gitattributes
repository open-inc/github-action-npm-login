# The bundle in dist/ is built on Linux runners and compared byte-for-byte by
# the check-dist job, so line endings must not depend on the checkout platform.
* text=auto eol=lf

# Generated by ncc - collapse in review diffs, exclude from language stats.
dist/** linguist-generated=true
